README.md (готовый текст)
# DevOps Lab: AWS EKS + Terraform + GitHub Actions

Учебно-боевой проект для отработки продовой инфраструктуры на AWS.  
Стек: **Terraform, EKS, Helm, ALB Ingress, GitHub Actions, ECR, RDS PostgreSQL, Loki, Promtail, Grafana**.

Проект имитирует полноценный пайплайн деплоя и обслуживания приложения в Kubernetes на AWS, включая CI/CD, мониторинг, логирование и автоскейлинг.

---

## ⚙ Архитектура

**Компоненты:**

- **VPC** (public/private subnets, NAT)
- **EKS Cluster** (managed node groups)
- **AWS Load Balancer Controller** (ALB для Ingress)
- **RDS PostgreSQL** (Private subnets)
- **ECR** (Docker Registry)
- **CI/CD через GitHub Actions**
- **Helm** (deployment demo-app)
- **Loki + Promtail + Grafana** (логирование и мониторинг)
- **IAM Roles for Service Accounts (IRSA)**  
- **HTTPS + Route 53 + ACM** (домен уровня прод)

---

## 🚀 Что делает CI/CD

При каждом пуше в `main`:

1. Собирает Docker image из `app/`
2. Пушит его в ECR
3. Выполняет:
   ```bash
   helm upgrade --install demo-app ./demo-app \
     --set image.repository=... \
     --set image.tag=...


Обновляет Deployment в EKS

Результат: любое изменение приложения автоматически попадает в кластер.

🏗 Основные модули Terraform
/terraform
  /vpc
  /eks
  /rds
  /iam
  /alb-controller
  /monitoring


Каждый модуль отвечает за отдельную часть инфраструктуры.
terraform apply поднимает весь стек автоматически.

🌐 Домен и HTTPS

Используется:

Route 53 для DNS

ACM для TLS-сертификатов

ALB Ingress Controller для маршрутизации трафика

После деплоя приложение доступно по адресу:

https://app.<ваш_домен>


Замочек зелёный, всё как у людей.

🧩 Demo-приложение

Простое Node.js/Express API с подключением к RDS PostgreSQL.
Использует библиотеку pg.

Ход работы:

Поднимается Deployment

Service отдаёт трафик

Ingress маршрутизирует через AWS ALB

📊 Мониторинг и Логи

Установлены:

Loki – сбор логов

Promtail – агент на нодах

Grafana – визуализация

Добавлены dashboards:

Kubernetes Cluster

Node Exporter

Pod CPU/RAM

📈 Авто-масштабирование

metrics-server

HPA для приложения

Cluster Autoscaler с корректной IAM ролью

Поведение:

нагрузка → HPA увеличивает поды → CA добавляет ноды в NodeGroup.

🔒 Secrets

Используется:

AWS Secrets Manager (как рекомендованный способ)

Secrets Store CSI Driver (Kubernetes)

YAML с паролями не храним.
Секреты подтягиваются динамически в поды.

🛠 Как запустить

Настроить AWS CLI:

aws configure


Инициализировать Terraform:

terraform init
terraform apply


Проверить кластер:

kubectl get nodes
kubectl get pods -A


Деплой приложения:

helm upgrade --install demo-app ./demo-app

📂 Структура репозитория
.
├── app/                   # Node.js приложение
├── demo-app/              # Helm chart
├── terraform/             # IaC инфраструктуры
├── .github/workflows/     # CI/CD пайплайны
└── README.md

🧹 Git Hygiene

Чтобы не залить секреты:

.gitignore настроен на terraform.tfvars, *.pem, *secret*, *.yaml с конфигами


Если секреты случайно попали в историю, используется:

git filter-repo --path ...

🧾 Лицензия

Свободное использование для обучения и экспериментов.
За прод в компании тебя заставят переписать половину, но это уже другая история.

